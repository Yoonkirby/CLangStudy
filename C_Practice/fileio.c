#include <stdio.h>

int main(void)
{
    /*FILE * fp = fopen("data.txt","wt");   //파일에 쓰기
    if(fp==NULL){
        puts("File open fail!");
        return -1;  // 비정상적 종료를 의미하기 위해서 -1을 반환
    }

    fputc('A', fp);
    fputc('B', fp);
    fputc('C', fp);
    fclose(fp);*/

    /*int ch, i;                            //파일에서 읽어오기
    FILE * fp = fopen("data.txt", "rt");
    if(fp==NULL){
        puts("File open fail!");
        return -1;  // 비정상적 종료를 의미하기 위해서 -1을 반환
    }

    for(i = 0; i < 3; i++){
        ch = fgetc(fp);
        printf("%c \n", ch);
    }
    fclose(fp);*/

    return 0;
}